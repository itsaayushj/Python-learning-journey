print("lets calculate the area of a Rectangle!!!!!")
length = float(input("Enter the Length of the rectangle"))
width = float(input("Enter the Width of the rectangle"))
area = width * length
print(f"The area of your rectangle is {area}cm²")
# alt + 0178 in number pad for that little two. useless shit.
# the error i made : using int in place of float.... using int will show error if a user will enter something like "69.69" and he can only add whole number. so i should use float. 
